rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 개발 단계: 모든 데이터에 대한 완전한 접근 허용
    match /{document=**} {
      allow read, write, delete: if true;
    }
  }
}
