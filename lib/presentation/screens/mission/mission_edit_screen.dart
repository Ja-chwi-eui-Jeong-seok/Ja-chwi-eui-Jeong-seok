import 'package:flutter/material.dart';

class MissionEditScreen extends StatelessWidget {
  const MissionEditScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('MissionEditScreen')),
      body: const Center(
        child: Text('MissionEditScreen Screen'),
      ),
    );
  }
}
