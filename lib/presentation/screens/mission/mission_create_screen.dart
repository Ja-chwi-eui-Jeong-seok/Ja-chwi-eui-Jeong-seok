import 'package:flutter/material.dart';

class MissionCreateScreen extends StatelessWidget {
  const MissionCreateScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('MissionCreateScreen')),
      body: const Center(
        child: Text('MissionCreateScreen Screen'),
      ),
    );
  }
}
